#!/usr/bin/python

from random import *

prob = "problem name"

cases = [   # 
        ]
cur = 0
for  in cases :
    cout = ""
    cur += 1
    print "make %d..." % cur
##-----
#TODO generate the data



##-----
    f = file( prob + str(cur) + ".in", "w" )
    f.write( cout )
    f.close()

